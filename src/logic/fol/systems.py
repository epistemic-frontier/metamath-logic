# GENERATED by mm-transpiler from replayed set.mm proofs.
from __future__ import annotations

from collections.abc import Callable, Mapping
from typing import TypeAlias

from skfd.proof import Proof, ProofBuilder, SystemCore

System: TypeAlias = SystemCore

LemmaCtor = Callable[[System], Proof]


def prove_barbara(sys: System) -> Proof:
    """"Barbara", one of the fundamental syllogisms of Aristotelian logic.  All
    ` ph ` is ` ps ` , and all ` ch ` is ` ph ` , therefore all ` ch ` is
    ` ps ` .  In Aristotelian notation, AAA-1:  MaP and SaM therefore SaP.
    For example, given "All men are mortal" and "Socrates is a man", we can
    prove "Socrates is mortal".  If H is the set of men, M is the set of
    mortal beings, and S is Socrates, these word phrases can be represented
    as ` A. x ( x e. H -> x e. M ) ` (all men are mortal) and
    ` A. x ( x = S -> x e. H ) ` (Socrates is a man) therefore
    ` A. x ( x = S -> x e. M ) ` (Socrates is mortal).  Russell and
    Whitehead note that "the syllogism in Barbara [[ ~ barbara ] is derived
    from [[ ~ syl ]" (quote after Theorem *2.06 of [WhiteheadRussell]
    p. 101).  Most of the proof is in ~ alsyl .  There are a legion of
    sources for Barbara, including ~ http://www.friesian.com/aristotl.htm ,
    ~ http://plato.stanford.edu/entries/aristotle-logic/ , and
    ~ https://en.wikipedia.org/wiki/Syllogism .  (Contributed by David A.
    Wheeler, 24-Aug-2016.)
    """
    lb = ProofBuilder(sys, "barbara")
    h1 = lb.hyp("barbara.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("barbara.min", "∀ x ( χ → φ )")
    s1 = lb.ref("s1", "( ( ∀ x ( χ → φ ) ∧ ∀ x ( φ → ψ ) ) → ∀ x ( χ → ψ ) )", ref="alsyl", note="alsyl")
    res = lb.ref("res", "∀ x ( χ → ψ )", h2, h1, s1, ref="mp2an", note="mp2an")
    return lb.build(res)

def prove_celarent(sys: System) -> Proof:
    """"Celarent", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , and all ` ch ` is ` ph ` , therefore no ` ch ` is ` ps ` .
    Instance of ~ barbara .  In Aristotelian notation, EAE-1:  MeP and SaM
    therefore SeP. For example, given the "No reptiles have fur" and "All
    snakes are reptiles", therefore "No snakes have fur".  Example from
    ~ https://en.wikipedia.org/wiki/Syllogism .  (Contributed by David A.
    Wheeler, 24-Aug-2016.)
    """
    lb = ProofBuilder(sys, "celarent")
    h1 = lb.hyp("celarent.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("celarent.min", "∀ x ( χ → φ )")
    res = lb.ref("res", "∀ x ( χ → ¬ ψ )", h1, h2, ref="barbara", note="barbara")
    return lb.build(res)

def prove_darii(sys: System) -> Proof:
    """"Darii", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , and some ` ch ` is ` ph ` , therefore some ` ch ` is ` ps ` .
    In Aristotelian notation, AII-1:  MaP and SiM therefore SiP. For
    example, given "All rabbits have fur" and "Some pets are rabbits",
    therefore "Some pets have fur".  Example from
    ~ https://en.wikipedia.org/wiki/Syllogism .  See ~ dariiALT for a
    shorter proof requiring more axioms.  (Contributed by David A. Wheeler,
    24-Aug-2016.)  Reduce dependencies on axioms.  (Revised by BJ,
    16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "darii")
    h1 = lb.hyp("darii.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("darii.min", "∃ x ( χ ∧ φ )")
    s1 = lb.ref("s1", "( ( φ → ψ ) → ( φ → ψ ) )", ref="id", note="id")
    s2 = lb.ref("s2", "( ( φ → ψ ) → ( ( χ ∧ φ ) → ( χ ∧ ψ ) ) )", s1, ref="anim2d", note="anim2d")
    s3 = lb.ref("s3", "( ∀ x ( φ → ψ ) → ∀ x ( ( χ ∧ φ ) → ( χ ∧ ψ ) ) )", s2, ref="alimi", note="alimi")
    s4 = lb.mp("s4", h1, s3, "ax-mp")
    s5 = lb.ref("s5", "( ∀ x ( ( χ ∧ φ ) → ( χ ∧ ψ ) ) → ( ∃ x ( χ ∧ φ ) → ∃ x ( χ ∧ ψ ) ) )", ref="exim", note="exim")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", s4, h2, s5, ref="mp2", note="mp2")
    return lb.build(res)

def prove_dariiALT(sys: System) -> Proof:
    """Alternate proof of ~ darii , shorter but using more axioms.  This shows
    how the use of ~ spi may shorten some proofs of the Aristotelian
    syllogisms, even though this adds axiom dependencies.  Note that ~ spi
    is the inference associated with ~ sp , which corresponds to the axiom
    (T) of modal logic.  (Contributed by David A. Wheeler, 27-Aug-2016.)
    Added precisions on axiom usage.  (Revised by BJ, 27-Sep-2022.)
    (Proof modification is discouraged.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "dariiALT")
    h1 = lb.hyp("darii.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("darii.min", "∃ x ( χ ∧ φ )")
    s1 = lb.ref("s1", "( φ → ψ )", h1, ref="spi", note="spi")
    s2 = lb.ref("s2", "( ( χ ∧ φ ) → ( χ ∧ ψ ) )", s1, ref="anim2i", note="anim2i")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", h2, s2, ref="eximii", note="eximii")
    return lb.build(res)

def prove_ferio(sys: System) -> Proof:
    """"Ferio" ("Ferioque"), one of the syllogisms of Aristotelian logic.  No
    ` ph ` is ` ps ` , and some ` ch ` is ` ph ` , therefore some ` ch ` is
    not ` ps ` .  Instance of ~ darii .  In Aristotelian notation, EIO-1:
    MeP and SiM therefore SoP. For example, given "No homework is fun" and
    "Some reading is homework", therefore "Some reading is not fun".  This
    is essentially a logical axiom in Aristotelian logic.  Example from
    ~ https://en.wikipedia.org/wiki/Syllogism .  (Contributed by David A.
    Wheeler, 24-Aug-2016.)
    """
    lb = ProofBuilder(sys, "ferio")
    h1 = lb.hyp("ferio.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("ferio.min", "∃ x ( χ ∧ φ )")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ ψ )", h1, h2, ref="darii", note="darii")
    return lb.build(res)

def prove_barbarilem(sys: System) -> Proof:
    """Lemma for ~ barbari and the other Aristotelian syllogisms with
    existential assumption.  (Contributed by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "barbarilem")
    h1 = lb.hyp("barbarilem.min", "∃ x φ")
    h2 = lb.hyp("barbarilem.maj", "∀ x ( φ → ψ )")
    s1 = lb.ref("s1", "( ∀ x ( φ → ψ ) → ( ∃ x φ → ∃ x ( φ ∧ ψ ) ) )", ref="exintr", note="exintr")
    res = lb.ref("res", "∃ x ( φ ∧ ψ )", h2, h1, s1, ref="mp2", note="mp2")
    return lb.build(res)

def prove_barbari(sys: System) -> Proof:
    """"Barbari", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , all ` ch ` is ` ph ` , and some ` ch ` exist, therefore some
    ` ch ` is ` ps ` .  In Aristotelian notation, AAI-1:  MaP and SaM
    therefore SiP. For example, given "All men are mortal", "All Greeks are
    men", and "Greeks exist", therefore "Some Greeks are mortal".  Note the
    existence hypothesis (to prove the "some" in the conclusion).  Example
    from ~ https://en.wikipedia.org/wiki/Syllogism .  (Contributed by David
    A. Wheeler, 27-Aug-2016.)  Reduce dependencies on axioms.  (Revised by
    BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "barbari")
    h1 = lb.hyp("barbari.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("barbari.min", "∀ x ( χ → φ )")
    h3 = lb.hyp("barbari.e", "∃ x χ")
    s1 = lb.ref("s1", "∀ x ( χ → ψ )", h1, h2, ref="barbara", note="barbara")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", h3, s1, ref="barbarilem", note="barbarilem")
    return lb.build(res)

def prove_barbariALT(sys: System) -> Proof:
    """Alternate proof of ~ barbari , shorter but using more axioms.  See
    comment of ~ dariiALT .  (Contributed by David A. Wheeler, 27-Aug-2016.)
    (Proof modification is discouraged.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "barbariALT")
    h1 = lb.hyp("barbari.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("barbari.min", "∀ x ( χ → φ )")
    h3 = lb.hyp("barbari.e", "∃ x χ")
    s1 = lb.ref("s1", "∀ x ( χ → ψ )", h1, h2, ref="barbara", note="barbara")
    s2 = lb.ref("s2", "( χ → ψ )", s1, ref="spi", note="spi")
    s3 = lb.ref("s3", "( χ → ( χ ∧ ψ ) )", s2, ref="ancli", note="ancli")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", h3, s3, ref="eximii", note="eximii")
    return lb.build(res)

def prove_celaront(sys: System) -> Proof:
    """"Celaront", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , all ` ch ` is ` ph ` , and some ` ch ` exist, therefore some
    ` ch ` is not ` ps ` .  Instance of ~ barbari .  In Aristotelian
    notation, EAO-1:  MeP and SaM therefore SoP. For example, given "No
    reptiles have fur", "All snakes are reptiles", and "Snakes exist", prove
    "Some snakes have no fur".  Note the existence hypothesis.  Example from
    ~ https://en.wikipedia.org/wiki/Syllogism .  (Contributed by David A.
    Wheeler, 27-Aug-2016.)
    """
    lb = ProofBuilder(sys, "celaront")
    h1 = lb.hyp("celaront.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("celaront.min", "∀ x ( χ → φ )")
    h3 = lb.hyp("celaront.e", "∃ x χ")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ ψ )", h1, h2, h3, ref="barbari", note="barbari")
    return lb.build(res)

def prove_cesare(sys: System) -> Proof:
    """"Cesare", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , and all ` ch ` is ` ps ` , therefore no ` ch ` is ` ph ` .  In
    Aristotelian notation, EAE-2:  PeM and SaM therefore SeP. Related to
    ~ celarent .  (Contributed by David A. Wheeler, 27-Aug-2016.)  Reduce
    dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "cesare")
    h1 = lb.hyp("cesare.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("cesare.min", "∀ x ( χ → ψ )")
    s1 = lb.ref("s1", "( ( φ → ¬ ψ ) → ( ψ → ¬ φ ) )", ref="con2", note="con2")
    s2 = lb.ref("s2", "( ∀ x ( φ → ¬ ψ ) → ∀ x ( ψ → ¬ φ ) )", s1, ref="alimi", note="alimi")
    s3 = lb.mp("s3", h1, s2, "ax-mp")
    res = lb.ref("res", "∀ x ( χ → ¬ φ )", s3, h2, ref="celarent", note="celarent")
    return lb.build(res)

def prove_camestres(sys: System) -> Proof:
    """"Camestres", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , and no ` ch ` is ` ps ` , therefore no ` ch ` is ` ph ` .  In
    Aristotelian notation, AEE-2:  PaM and SeM therefore SeP. (Contributed
    by David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on axioms.
    (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "camestres")
    h1 = lb.hyp("camestres.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("camestres.min", "∀ x ( χ → ¬ ψ )")
    s1 = lb.ref("s1", "( ( φ → ψ ) → ( ¬ ψ → ¬ φ ) )", ref="con3", note="con3")
    s2 = lb.ref("s2", "( ∀ x ( φ → ψ ) → ∀ x ( ¬ ψ → ¬ φ ) )", s1, ref="alimi", note="alimi")
    s3 = lb.mp("s3", h1, s2, "ax-mp")
    res = lb.ref("res", "∀ x ( χ → ¬ φ )", s3, h2, ref="celarent", note="celarent")
    return lb.build(res)

def prove_festino(sys: System) -> Proof:
    """"Festino", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , and some ` ch ` is ` ps ` , therefore some ` ch ` is not
    ` ph ` .  In Aristotelian notation, EIO-2:  PeM and SiM therefore SoP.
    (Contributed by David A. Wheeler, 25-Nov-2016.)  Reduce dependencies on
    axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "festino")
    h1 = lb.hyp("festino.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("festino.min", "∃ x ( χ ∧ ψ )")
    s1 = lb.ref("s1", "( ( φ → ¬ ψ ) → ( ψ → ¬ φ ) )", ref="con2", note="con2")
    s2 = lb.ref("s2", "( ( φ → ¬ ψ ) → ( ( χ ∧ ψ ) → ( χ ∧ ¬ φ ) ) )", s1, ref="anim2d", note="anim2d")
    s3 = lb.ref("s3", "( ∀ x ( φ → ¬ ψ ) → ∀ x ( ( χ ∧ ψ ) → ( χ ∧ ¬ φ ) ) )", s2, ref="alimi", note="alimi")
    s4 = lb.mp("s4", h1, s3, "ax-mp")
    s5 = lb.ref("s5", "( ∀ x ( ( χ ∧ ψ ) → ( χ ∧ ¬ φ ) ) → ( ∃ x ( χ ∧ ψ ) → ∃ x ( χ ∧ ¬ φ ) ) )", ref="exim", note="exim")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", s4, h2, s5, ref="mp2", note="mp2")
    return lb.build(res)

def prove_festinoALT(sys: System) -> Proof:
    """Alternate proof of ~ festino , shorter but using more axioms.  See
    comment of ~ dariiALT .  (Contributed by David A. Wheeler, 27-Aug-2016.)
    (Proof modification is discouraged.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "festinoALT")
    h1 = lb.hyp("festino.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("festino.min", "∃ x ( χ ∧ ψ )")
    s1 = lb.ref("s1", "( φ → ¬ ψ )", h1, ref="spi", note="spi")
    s2 = lb.ref("s2", "( ψ → ¬ φ )", s1, ref="con2i", note="con2i")
    s3 = lb.ref("s3", "( ( χ ∧ ψ ) → ( χ ∧ ¬ φ ) )", s2, ref="anim2i", note="anim2i")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h2, s3, ref="eximii", note="eximii")
    return lb.build(res)

def prove_baroco(sys: System) -> Proof:
    """"Baroco", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , and some ` ch ` is not ` ps ` , therefore some ` ch ` is not
    ` ph ` .  In Aristotelian notation, AOO-2:  PaM and SoM therefore SoP.
    For example, "All informative things are useful", "Some websites are not
    useful", therefore "Some websites are not informative".  (Contributed by
    David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on axioms.
    (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "baroco")
    h1 = lb.hyp("baroco.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("baroco.min", "∃ x ( χ ∧ ¬ ψ )")
    s1 = lb.ref("s1", "( ( φ → ψ ) → ( ¬ ψ → ¬ φ ) )", ref="con3", note="con3")
    s2 = lb.ref("s2", "( ( φ → ψ ) → ( ( χ ∧ ¬ ψ ) → ( χ ∧ ¬ φ ) ) )", s1, ref="anim2d", note="anim2d")
    s3 = lb.ref("s3", "( ∀ x ( φ → ψ ) → ∀ x ( ( χ ∧ ¬ ψ ) → ( χ ∧ ¬ φ ) ) )", s2, ref="alimi", note="alimi")
    s4 = lb.mp("s4", h1, s3, "ax-mp")
    s5 = lb.ref("s5", "( ∀ x ( ( χ ∧ ¬ ψ ) → ( χ ∧ ¬ φ ) ) → ( ∃ x ( χ ∧ ¬ ψ ) → ∃ x ( χ ∧ ¬ φ ) ) )", ref="exim", note="exim")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", s4, h2, s5, ref="mp2", note="mp2")
    return lb.build(res)

def prove_barocoALT(sys: System) -> Proof:
    """Alternate proof of ~ festino , shorter but using more axioms.  See
    comment of ~ dariiALT .  (Contributed by David A. Wheeler, 27-Aug-2016.)
    (Proof modification is discouraged.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "barocoALT")
    h1 = lb.hyp("baroco.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("baroco.min", "∃ x ( χ ∧ ¬ ψ )")
    s1 = lb.ref("s1", "( φ → ψ )", h1, ref="spi", note="spi")
    s2 = lb.ref("s2", "( ¬ ψ → ¬ φ )", s1, ref="con3i", note="con3i")
    s3 = lb.ref("s3", "( ( χ ∧ ¬ ψ ) → ( χ ∧ ¬ φ ) )", s2, ref="anim2i", note="anim2i")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h2, s3, ref="eximii", note="eximii")
    return lb.build(res)

def prove_cesaro(sys: System) -> Proof:
    """"Cesaro", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , all ` ch ` is ` ps ` , and ` ch ` exist, therefore some ` ch `
    is not ` ph ` .  In Aristotelian notation, EAO-2:  PeM and SaM therefore
    SoP. (Contributed by David A. Wheeler, 28-Aug-2016.)  Reduce
    dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "cesaro")
    h1 = lb.hyp("cesaro.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("cesaro.min", "∀ x ( χ → ψ )")
    h3 = lb.hyp("cesaro.e", "∃ x χ")
    s1 = lb.ref("s1", "∀ x ( χ → ¬ φ )", h1, h2, ref="cesare", note="cesare")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h3, s1, ref="barbarilem", note="barbarilem")
    return lb.build(res)

def prove_camestros(sys: System) -> Proof:
    """"Camestros", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , no ` ch ` is ` ps ` , and ` ch ` exist, therefore some ` ch `
    is not ` ph ` .  In Aristotelian notation, AEO-2:  PaM and SeM therefore
    SoP. For example, "All horses have hooves", "No humans have hooves", and
    humans exist, therefore "Some humans are not horses".  (Contributed by
    David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on axioms.
    (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "camestros")
    h1 = lb.hyp("camestros.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("camestros.min", "∀ x ( χ → ¬ ψ )")
    h3 = lb.hyp("camestros.e", "∃ x χ")
    s1 = lb.ref("s1", "∀ x ( χ → ¬ φ )", h1, h2, ref="camestres", note="camestres")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h3, s1, ref="barbarilem", note="barbarilem")
    return lb.build(res)

def prove_datisi(sys: System) -> Proof:
    """"Datisi", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , and some ` ph ` is ` ch ` , therefore some ` ch ` is ` ps ` .
    In Aristotelian notation, AII-3:  MaP and MiS therefore SiP.
    (Contributed by David A. Wheeler, 28-Aug-2016.)  Shorten and reduce
    dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "datisi")
    h1 = lb.hyp("datisi.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("datisi.min", "∃ x ( φ ∧ χ )")
    s1 = lb.ref("s1", "( ∃ x ( φ ∧ χ ) ↔ ∃ x ( χ ∧ φ ) )", ref="exancom", note="exancom")
    s2 = lb.ref("s2", "∃ x ( χ ∧ φ )", h2, s1, ref="mpbi", note="mpbi")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", h1, s2, ref="darii", note="darii")
    return lb.build(res)

def prove_disamis(sys: System) -> Proof:
    """"Disamis", one of the syllogisms of Aristotelian logic.  Some ` ph ` is
    ` ps ` , and all ` ph ` is ` ch ` , therefore some ` ch ` is ` ps ` .
    In Aristotelian notation, IAI-3:  MiP and MaS therefore SiP.
    (Contributed by David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on
    axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "disamis")
    h1 = lb.hyp("disamis.maj", "∃ x ( φ ∧ ψ )")
    h2 = lb.hyp("disamis.min", "∀ x ( φ → χ )")
    s1 = lb.ref("s1", "∃ x ( ψ ∧ χ )", h2, h1, ref="datisi", note="datisi")
    s2 = lb.ref("s2", "( ∃ x ( ψ ∧ χ ) ↔ ∃ x ( χ ∧ ψ ) )", ref="exancom", note="exancom")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", s1, s2, ref="mpbi", note="mpbi")
    return lb.build(res)

def prove_ferison(sys: System) -> Proof:
    """"Ferison", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , and some ` ph ` is ` ch ` , therefore some ` ch ` is not
    ` ps ` .  Instance of ~ datisi .  In Aristotelian notation, EIO-3:  MeP
    and MiS therefore SoP. (Contributed by David A. Wheeler,
    28-Aug-2016.)
    """
    lb = ProofBuilder(sys, "ferison")
    h1 = lb.hyp("ferison.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("ferison.min", "∃ x ( φ ∧ χ )")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ ψ )", h1, h2, ref="datisi", note="datisi")
    return lb.build(res)

def prove_bocardo(sys: System) -> Proof:
    """"Bocardo", one of the syllogisms of Aristotelian logic.  Some ` ph ` is
    not ` ps ` , and all ` ph ` is ` ch ` , therefore some ` ch ` is not
    ` ps ` .  Instance of ~ disamis .  In Aristotelian notation, OAO-3:  MoP
    and MaS therefore SoP. For example, "Some cats have no tails", "All cats
    are mammals", therefore "Some mammals have no tails".  (Contributed by
    David A. Wheeler, 28-Aug-2016.)
    """
    lb = ProofBuilder(sys, "bocardo")
    h1 = lb.hyp("bocardo.maj", "∃ x ( φ ∧ ¬ ψ )")
    h2 = lb.hyp("bocardo.min", "∀ x ( φ → χ )")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ ψ )", h1, h2, ref="disamis", note="disamis")
    return lb.build(res)

def prove_darapti(sys: System) -> Proof:
    """"Darapti", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , all ` ph ` is ` ch ` , and some ` ph ` exist, therefore some
    ` ch ` is ` ps ` .  In Aristotelian notation, AAI-3:  MaP and MaS
    therefore SiP. For example, "All squares are rectangles" and "All
    squares are rhombuses", therefore "Some rhombuses are rectangles".
    (Contributed by David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on
    axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "darapti")
    h1 = lb.hyp("darapti.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("darapti.min", "∀ x ( φ → χ )")
    h3 = lb.hyp("darapti.e", "∃ x φ")
    s1 = lb.ref("s1", "( ( ( φ → χ ) ∧ ( φ → ψ ) ) → ( ( φ → χ ) ∧ ( φ → ψ ) ) )", ref="id", note="id")
    s2 = lb.ref("s2", "( ( ∀ x ( φ → χ ) ∧ ∀ x ( φ → ψ ) ) → ∀ x ( ( φ → χ ) ∧ ( φ → ψ ) ) )", s1, ref="alanimi", note="alanimi")
    s3 = lb.ref("s3", "∀ x ( ( φ → χ ) ∧ ( φ → ψ ) )", h2, h1, s2, ref="mp2an", note="mp2an")
    s4 = lb.ref("s4", "( ( ( φ → χ ) ∧ ( φ → ψ ) ) → ( φ → ( χ ∧ ψ ) ) )", ref="pm3.43", note="pm3.43")
    s5 = lb.ref("s5", "( ∀ x ( ( φ → χ ) ∧ ( φ → ψ ) ) → ∀ x ( φ → ( χ ∧ ψ ) ) )", s4, ref="alimi", note="alimi")
    s6 = lb.mp("s6", s3, s5, "ax-mp")
    s7 = lb.ref("s7", "( ∀ x ( φ → ( χ ∧ ψ ) ) → ( ∃ x φ → ∃ x ( χ ∧ ψ ) ) )", ref="exim", note="exim")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", s6, h3, s7, ref="mp2", note="mp2")
    return lb.build(res)

def prove_daraptiALT(sys: System) -> Proof:
    """Alternate proof of ~ darapti , shorter but using more axioms.  See
    comment of ~ dariiALT .  (Contributed by David A. Wheeler, 27-Aug-2016.)
    (Proof modification is discouraged.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "daraptiALT")
    h1 = lb.hyp("darapti.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("darapti.min", "∀ x ( φ → χ )")
    h3 = lb.hyp("darapti.e", "∃ x φ")
    s1 = lb.ref("s1", "( φ → χ )", h2, ref="spi", note="spi")
    s2 = lb.ref("s2", "( φ → ψ )", h1, ref="spi", note="spi")
    s3 = lb.ref("s3", "( φ → ( χ ∧ ψ ) )", s1, s2, ref="jca", note="jca")
    res = lb.ref("res", "∃ x ( χ ∧ ψ )", h3, s3, ref="eximii", note="eximii")
    return lb.build(res)

def prove_felapton(sys: System) -> Proof:
    """"Felapton", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , all ` ph ` is ` ch ` , and some ` ph ` exist, therefore some
    ` ch ` is not ` ps ` .  Instance of ~ darapti .  In Aristotelian
    notation, EAO-3:  MeP and MaS therefore SoP. For example, "No flowers
    are animals" and "All flowers are plants", therefore "Some plants are
    not animals".  (Contributed by David A. Wheeler, 28-Aug-2016.)
    """
    lb = ProofBuilder(sys, "felapton")
    h1 = lb.hyp("felapton.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("felapton.min", "∀ x ( φ → χ )")
    h3 = lb.hyp("felapton.e", "∃ x φ")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ ψ )", h1, h2, h3, ref="darapti", note="darapti")
    return lb.build(res)

def prove_calemes(sys: System) -> Proof:
    """"Calemes", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , and no ` ps ` is ` ch ` , therefore no ` ch ` is ` ph ` .  In
    Aristotelian notation, AEE-4:  PaM and MeS therefore SeP. (Contributed
    by David A. Wheeler, 28-Aug-2016.)  Reduce dependencies on axioms.
    (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "calemes")
    h1 = lb.hyp("calemes.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("calemes.min", "∀ x ( ψ → ¬ χ )")
    s1 = lb.ref("s1", "( ( ψ → ¬ χ ) → ( χ → ¬ ψ ) )", ref="con2", note="con2")
    s2 = lb.ref("s2", "( ∀ x ( ψ → ¬ χ ) → ∀ x ( χ → ¬ ψ ) )", s1, ref="alimi", note="alimi")
    s3 = lb.mp("s3", h2, s2, "ax-mp")
    res = lb.ref("res", "∀ x ( χ → ¬ φ )", h1, s3, ref="camestres", note="camestres")
    return lb.build(res)

def prove_dimatis(sys: System) -> Proof:
    """"Dimatis", one of the syllogisms of Aristotelian logic.  Some ` ph ` is
    ` ps ` , and all ` ps ` is ` ch ` , therefore some ` ch ` is ` ph ` .
    In Aristotelian notation, IAI-4:  PiM and MaS therefore SiP. For
    example, "Some pets are rabbits", "All rabbits have fur", therefore
    "Some fur bearing animals are pets".  Like ~ darii with positions
    interchanged.  (Contributed by David A. Wheeler, 28-Aug-2016.)  Shorten
    and reduce dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "dimatis")
    h1 = lb.hyp("dimatis.maj", "∃ x ( φ ∧ ψ )")
    h2 = lb.hyp("dimatis.min", "∀ x ( ψ → χ )")
    s1 = lb.ref("s1", "∃ x ( φ ∧ χ )", h2, h1, ref="darii", note="darii")
    s2 = lb.ref("s2", "( ∃ x ( φ ∧ χ ) ↔ ∃ x ( χ ∧ φ ) )", ref="exancom", note="exancom")
    res = lb.ref("res", "∃ x ( χ ∧ φ )", s1, s2, ref="mpbi", note="mpbi")
    return lb.build(res)

def prove_fresison(sys: System) -> Proof:
    """"Fresison", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` (PeM), and some ` ps ` is ` ch ` (MiS), therefore some ` ch ` is
    not ` ph ` (SoP).  In Aristotelian notation, EIO-4:  PeM and MiS
    therefore SoP. (Contributed by David A. Wheeler, 28-Aug-2016.)  Shorten
    and reduce dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "fresison")
    h1 = lb.hyp("fresison.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("fresison.min", "∃ x ( ψ ∧ χ )")
    s1 = lb.ref("s1", "( ∃ x ( ψ ∧ χ ) ↔ ∃ x ( χ ∧ ψ ) )", ref="exancom", note="exancom")
    s2 = lb.ref("s2", "∃ x ( χ ∧ ψ )", h2, s1, ref="mpbi", note="mpbi")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h1, s2, ref="festino", note="festino")
    return lb.build(res)

def prove_calemos(sys: System) -> Proof:
    """"Calemos", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` (PaM), no ` ps ` is ` ch ` (MeS), and ` ch ` exist, therefore
    some ` ch ` is not ` ph ` (SoP).  In Aristotelian notation, AEO-4:  PaM
    and MeS therefore SoP. (Contributed by David A. Wheeler, 28-Aug-2016.)
    Shorten and reduce dependencies on axioms.  (Revised by BJ,
    16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "calemos")
    h1 = lb.hyp("calemos.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("calemos.min", "∀ x ( ψ → ¬ χ )")
    h3 = lb.hyp("calemos.e", "∃ x χ")
    s1 = lb.ref("s1", "∀ x ( χ → ¬ φ )", h1, h2, ref="calemes", note="calemes")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", h3, s1, ref="barbarilem", note="barbarilem")
    return lb.build(res)

def prove_fesapo(sys: System) -> Proof:
    """"Fesapo", one of the syllogisms of Aristotelian logic.  No ` ph ` is
    ` ps ` , all ` ps ` is ` ch ` , and ` ps ` exist, therefore some ` ch `
    is not ` ph ` .  In Aristotelian notation, EAO-4:  PeM and MaS therefore
    SoP. (Contributed by David A. Wheeler, 28-Aug-2016.)  Reduce
    dependencies on axioms.  (Revised by BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "fesapo")
    h1 = lb.hyp("fesapo.maj", "∀ x ( φ → ¬ ψ )")
    h2 = lb.hyp("fesapo.min", "∀ x ( ψ → χ )")
    h3 = lb.hyp("fesapo.e", "∃ x ψ")
    s1 = lb.ref("s1", "( ( φ → ¬ ψ ) → ( ψ → ¬ φ ) )", ref="con2", note="con2")
    s2 = lb.ref("s2", "( ∀ x ( φ → ¬ ψ ) → ∀ x ( ψ → ¬ φ ) )", s1, ref="alimi", note="alimi")
    s3 = lb.mp("s3", h1, s2, "ax-mp")
    res = lb.ref("res", "∃ x ( χ ∧ ¬ φ )", s3, h2, h3, ref="felapton", note="felapton")
    return lb.build(res)

def prove_bamalip(sys: System) -> Proof:
    """"Bamalip", one of the syllogisms of Aristotelian logic.  All ` ph ` is
    ` ps ` , all ` ps ` is ` ch ` , and ` ph ` exist, therefore some ` ch `
    is ` ph ` .  In Aristotelian notation, AAI-4:  PaM and MaS therefore
    SiP. Very similar to ~ barbari .  (Contributed by David A. Wheeler,
    28-Aug-2016.)  Shorten and reduce dependencies on axioms.  (Revised by
    BJ, 16-Sep-2022.)
    """
    lb = ProofBuilder(sys, "bamalip")
    h1 = lb.hyp("bamalip.maj", "∀ x ( φ → ψ )")
    h2 = lb.hyp("bamalip.min", "∀ x ( ψ → χ )")
    h3 = lb.hyp("bamalip.e", "∃ x φ")
    s1 = lb.ref("s1", "∃ x ( φ ∧ χ )", h2, h1, h3, ref="barbari", note="barbari")
    s2 = lb.ref("s2", "( ∃ x ( φ ∧ χ ) ↔ ∃ x ( χ ∧ φ ) )", ref="exancom", note="exancom")
    res = lb.ref("res", "∃ x ( χ ∧ φ )", s1, s2, ref="mpbi", note="mpbi")
    return lb.build(res)

def prove_axia1(sys: System) -> Proof:
    """Left 'and' elimination (intuitionistic logic axiom ax-ia1).  (Contributed
    by Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axia1")
    res = lb.ref("res", "( ( φ ∧ ψ ) → φ )", ref="simpl", note="simpl")
    return lb.build(res)

def prove_axia2(sys: System) -> Proof:
    """Right 'and' elimination (intuitionistic logic axiom ax-ia2).  (Contributed
    by Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axia2")
    res = lb.ref("res", "( ( φ ∧ ψ ) → ψ )", ref="simpr", note="simpr")
    return lb.build(res)

def prove_axia3(sys: System) -> Proof:
    """'And' introduction (intuitionistic logic axiom ax-ia3).  (Contributed by
    Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axia3")
    res = lb.ref("res", "( φ → ( ψ → ( φ ∧ ψ ) ) )", ref="pm3.2", note="pm3.2")
    return lb.build(res)

def prove_axin1(sys: System) -> Proof:
    """'Not' introduction (intuitionistic logic axiom ax-in1).  (Contributed by
    Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axin1")
    res = lb.ref("res", "( ( φ → ¬ φ ) → ¬ φ )", ref="pm2.01", note="pm2.01")
    return lb.build(res)

def prove_axin2(sys: System) -> Proof:
    """'Not' elimination (intuitionistic logic axiom ax-in2).  (Contributed by
    Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axin2")
    res = lb.ref("res", "( ¬ φ → ( φ → ψ ) )", ref="pm2.21", note="pm2.21")
    return lb.build(res)

def prove_axio(sys: System) -> Proof:
    """Definition of 'or' (intuitionistic logic axiom ax-io).  (Contributed by
    Jim Kingdon, 21-May-2018.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axio")
    res = lb.ref("res", "( ( ( φ ∨ χ ) → ψ ) ↔ ( ( φ → ψ ) ∧ ( χ → ψ ) ) )", ref="jaob", note="jaob")
    return lb.build(res)

def prove_axi4(sys: System) -> Proof:
    """Specialization (intuitionistic logic axiom ax-4).  This is just ~ sp by
    another name.  (Contributed by Jim Kingdon, 31-Dec-2017.)
    (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axi4")
    res = lb.ref("res", "( ∀ x φ → φ )", ref="sp", note="sp")
    return lb.build(res)

def prove_axi5r(sys: System) -> Proof:
    """Converse of ~ axc4 (intuitionistic logic axiom ax-i5r).  (Contributed by
    Jim Kingdon, 31-Dec-2017.)
    """
    lb = ProofBuilder(sys, "axi5r")
    s1 = lb.ref("s1", "( ∀ x φ → ∀ x ∀ x φ )", ref="hba1", note="hba1")
    s2 = lb.ref("s2", "( ∀ x ψ → ∀ x ∀ x ψ )", ref="hba1", note="hba1")
    s3 = lb.ref("s3", "( ( ∀ x φ → ∀ x ψ ) → ∀ x ( ∀ x φ → ∀ x ψ ) )", s1, s2, ref="hbim", note="hbim")
    s4 = lb.ref("s4", "( ∀ x ψ → ψ )", ref="sp", note="sp")
    s5 = lb.ref("s5", "( ( ∀ x φ → ∀ x ψ ) → ( ∀ x φ → ψ ) )", s4, ref="imim2i", note="imim2i")
    res = lb.ref("res", "( ( ∀ x φ → ∀ x ψ ) → ∀ x ( ∀ x φ → ψ ) )", s3, s5, ref="alrimih", note="alrimih")
    return lb.build(res)

def prove_axial(sys: System) -> Proof:
    """The setvar ` x ` is not free in ` A. x ph ` (intuitionistic logic axiom
    ax-ial).  (Contributed by Jim Kingdon, 31-Dec-2017.)
    (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axial")
    res = lb.ref("res", "( ∀ x φ → ∀ x ∀ x φ )", ref="hba1", note="hba1")
    return lb.build(res)

def prove_axie1(sys: System) -> Proof:
    """The setvar ` x ` is not free in ` E. x ph ` (intuitionistic logic axiom
    ax-ie1).  (Contributed by Jim Kingdon, 31-Dec-2017.)
    (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axie1")
    res = lb.ref("res", "( ∃ x φ → ∀ x ∃ x φ )", ref="hbe1", note="hbe1")
    return lb.build(res)

def prove_axie2(sys: System) -> Proof:
    """A key property of existential quantification (intuitionistic logic axiom
    ax-ie2).  (Contributed by Jim Kingdon, 31-Dec-2017.)
    """
    lb = ProofBuilder(sys, "axie2")
    s1 = lb.ref("s1", "( F/ x ψ ↔ ∀ x ( ψ → ∀ x ψ ) )", ref="nf5", note="nf5")
    s2 = lb.ref("s2", "( F/ x ψ → ( ∀ x ( φ → ψ ) ↔ ( ∃ x φ → ψ ) ) )", ref="19.23t", note="19.23t")
    res = lb.ref("res", "( ∀ x ( ψ → ∀ x ψ ) → ( ∀ x ( φ → ψ ) ↔ ( ∃ x φ → ψ ) ) )", s1, s2, ref="sylbir", note="sylbir")
    return lb.build(res)

def prove_axi9(sys: System) -> Proof:
    """Axiom of existence (intuitionistic logic axiom ax-i9).  In classical
    logic, this is equivalent to ~ ax-6 but in intuitionistic logic it needs
    to be stated using the existential quantifier.  (Contributed by Jim
    Kingdon, 31-Dec-2017.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axi9")
    res = lb.ref("res", "∃ x x = y", ref="ax6e", note="ax6e")
    return lb.build(res)

def prove_axi10(sys: System) -> Proof:
    """Axiom of Quantifier Substitution (intuitionistic logic axiom ax-10).  This
    is just ~ axc11n by another name.  (Contributed by Jim Kingdon,
    31-Dec-2017.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axi10")
    res = lb.ref("res", "( ∀ x x = y → ∀ y y = x )", ref="axc11n", note="axc11n")
    return lb.build(res)

def prove_axi12(sys: System) -> Proof:
    """Axiom of Quantifier Introduction (intuitionistic logic axiom ax-i12).  In
    classical logic, this is mostly a restatement of ~ axc9 (with one
    additional quantifier).  But in intuitionistic logic, changing the
    negations and implications to disjunctions makes it stronger.  Usage of
    this theorem is discouraged because it depends on ~ ax-13 .  (Contributed
    by Jim Kingdon, 31-Dec-2017.)  Avoid ~ ax-11 .  (Revised by Wolf Lammen,
    24-Apr-2023.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axi12")
    s1 = lb.ref("s1", "F/ z ∀ z z = x", ref="nfa1", note="nfa1")
    s2 = lb.ref("s2", "F/ z ∀ z z = y", ref="nfa1", note="nfa1")
    s3 = lb.ref("s3", "F/ z ( ∀ z z = x ∨ ∀ z z = y )", s1, s2, ref="nfor", note="nfor")
    s4 = lb.ref("s4", "( ∀ z ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ( x = y → ∀ z x = y ) ) ↔ ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) ) )", s3, ref="19.32", note="19.32")
    s5 = lb.ref("s5", "( ¬ ∀ z z = x → ( ¬ ∀ z z = y → ( x = y → ∀ z x = y ) ) )", ref="axc9", note="axc9")
    s6 = lb.ref("s6", "( ¬ ∀ z z = x → ( ∀ z z = y ∨ ( x = y → ∀ z x = y ) ) )", s5, ref="orrd", note="orrd")
    s7 = lb.ref("s7", "( ∀ z z = x ∨ ( ∀ z z = y ∨ ( x = y → ∀ z x = y ) ) )", s6, ref="orri", note="orri")
    s8 = lb.ref("s8", "( ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ( x = y → ∀ z x = y ) ) ↔ ( ∀ z z = x ∨ ( ∀ z z = y ∨ ( x = y → ∀ z x = y ) ) ) )", ref="orass", note="orass")
    s9 = lb.ref("s9", "( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ( x = y → ∀ z x = y ) )", s7, s8, ref="mpbir", note="mpbir")
    s10 = lb.ref("s10", "( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) )", s4, s9, ref="mpgbi", note="mpgbi")
    s11 = lb.ref("s11", "( ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) ) ↔ ( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ z ( x = y → ∀ z x = y ) ) ) )", ref="orass", note="orass")
    res = lb.ref("res", "( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ z ( x = y → ∀ z x = y ) ) )", s10, s11, ref="mpbi", note="mpbi")
    return lb.build(res)

def prove_axbnd(sys: System) -> Proof:
    """Axiom of Bundling (intuitionistic logic axiom ax-bnd).  In classical
    logic, this and ~ axi12 are fairly straightforward consequences of
    ~ axc9 .  But in intuitionistic logic, it is not easy to add the extra
    ` A. x ` to ~ axi12 and so we treat the two as separate axioms.  Usage of
    this theorem is discouraged because it depends on ~ ax-13 .  (Contributed
    by Jim Kingdon, 22-Mar-2018.)  (Proof shortened by Wolf Lammen,
    24-Apr-2023.)  (New usage is discouraged.)
    """
    lb = ProofBuilder(sys, "axbnd")
    s1 = lb.ref("s1", "F/ x ∀ z z = x", ref="nfae", note="nfae")
    s2 = lb.ref("s2", "F/ x ∀ z z = y", ref="nfae", note="nfae")
    s3 = lb.ref("s3", "F/ x ( ∀ z z = x ∨ ∀ z z = y )", s1, s2, ref="nfor", note="nfor")
    s4 = lb.ref("s4", "( ∀ x ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) ) ↔ ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ x ∀ z ( x = y → ∀ z x = y ) ) )", s3, ref="19.32", note="19.32")
    s5 = lb.ref("s5", "( ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ x ∀ z ( x = y → ∀ z x = y ) ) ↔ ( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ x ∀ z ( x = y → ∀ z x = y ) ) ) )", ref="orass", note="orass")
    s6 = lb.ref("s6", "( ∀ x ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) ) ↔ ( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ x ∀ z ( x = y → ∀ z x = y ) ) ) )", s4, s5, ref="bitri", note="bitri")
    s7 = lb.ref("s7", "( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ z ( x = y → ∀ z x = y ) ) )", ref="axi12", note="axi12")
    s8 = lb.ref("s8", "( ( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) ) ↔ ( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ z ( x = y → ∀ z x = y ) ) ) )", ref="orass", note="orass")
    s9 = lb.ref("s9", "( ( ∀ z z = x ∨ ∀ z z = y ) ∨ ∀ z ( x = y → ∀ z x = y ) )", s7, s8, ref="mpbir", note="mpbir")
    res = lb.ref("res", "( ∀ z z = x ∨ ( ∀ z z = y ∨ ∀ x ∀ z ( x = y → ∀ z x = y ) ) )", s6, s9, ref="mpgbi", note="mpgbi")
    return lb.build(res)


THEOREMS: Mapping[str, LemmaCtor] = {
    'barbara': prove_barbara,
    'celarent': prove_celarent,
    'darii': prove_darii,
    'dariiALT': prove_dariiALT,
    'ferio': prove_ferio,
    'barbarilem': prove_barbarilem,
    'barbari': prove_barbari,
    'barbariALT': prove_barbariALT,
    'celaront': prove_celaront,
    'cesare': prove_cesare,
    'camestres': prove_camestres,
    'festino': prove_festino,
    'festinoALT': prove_festinoALT,
    'baroco': prove_baroco,
    'barocoALT': prove_barocoALT,
    'cesaro': prove_cesaro,
    'camestros': prove_camestros,
    'datisi': prove_datisi,
    'disamis': prove_disamis,
    'ferison': prove_ferison,
    'bocardo': prove_bocardo,
    'darapti': prove_darapti,
    'daraptiALT': prove_daraptiALT,
    'felapton': prove_felapton,
    'calemes': prove_calemes,
    'dimatis': prove_dimatis,
    'fresison': prove_fresison,
    'calemos': prove_calemos,
    'fesapo': prove_fesapo,
    'bamalip': prove_bamalip,
    'axia1': prove_axia1,
    'axia2': prove_axia2,
    'axia3': prove_axia3,
    'axin1': prove_axin1,
    'axin2': prove_axin2,
    'axio': prove_axio,
    'axi4': prove_axi4,
    'axi5r': prove_axi5r,
    'axial': prove_axial,
    'axie1': prove_axie1,
    'axie2': prove_axie2,
    'axi9': prove_axi9,
    'axi10': prove_axi10,
    'axi12': prove_axi12,
    'axbnd': prove_axbnd,
}

__all__ = ['THEOREMS']
